/* -------- GLOBAL VARIABLES -------- */

var gameIsOn = false;

/* -------- EVENT HANDLERS ---------- */

var startGame = function(e) {
    gameIsOn = true;
};

var stopGame = function(e) {
    gameIsOn = false;
};